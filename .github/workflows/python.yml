name: Run Python Linting

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  check:
    runs-on: ubuntu-latest

    env:
      DOCKER_RUN: "docker run -v $GITHUB_WORKSPACE:/fisheye --rm ghcr.io/mrjoe3012/fisheye-python:latest"

    permissions:
      packages: read 

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Docker login
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install
        run: |
          $DOCKER_RUN /bin/sh -c ". /venv/bin/activate && cd $GITHUB_WORKSPACE && pip install -e ./fisheye_python"

      - name: Pylint
        run: |
          $DOCKER_RUN /bin/sh -c ". /venv/bin/activate && pylint /github/workspace/fisheye_python/"

      - name: MyPy
        run: |
          $DOCKER_RUN /bin/sh -c ". /venv/bin/activate && mypy /github/workspace/fisheye_python/"

      - name: Pytest
        run: |
          $DOCKER_RUN /bin/sh -c ". /venv/bin/activate && pytest /github/workspace/fisheye_python/tests"
